# PAI v4.4.0 — System Atlas

> Complete structural map of the PAI system. Use this as the canonical reference for what exists, where it lives, and how it connects.

---

## Repository Structure

```
PersonalAI/
├── Releases/                    # 1 release (v4.4.0) — 11 old releases removed in v4.4.1-dev
│   └── v4.4.0/.claude/          # Current release — THE system
├── Tools/                       # Repo-level utilities (BackupRestore.ts, validate-protected.ts)
├── .github/                     # CI/CD workflows
├── images/                      # Repo images
├── README.md                    # Public repo README
├── PLATFORM.md                  # Platform overview
├── SECURITY.md                  # Security policy
├── ARCHITECTURAL_REVIEW.md      # Prior architectural review (20KB, stale)
├── PAI-v4.1.0-plan.md           # Historical planning doc
└── PAI-v4.1.0-summary.md        # Historical summary
```

**Release History (in Releases/):**

| Version | Size | Notes |
|---------|------|-------|
| v4.4.0 | 20MB | **Current release** — only version kept after cleanup |

**Note:** 11 old releases (v2.3 through v4.3.1, ~314MB) were removed during v4.4.1-dev cleanup. All history preserved in git. See IMPROVEMENT-INDEX.md §3.1.

---

## v4.4.0/.claude/ — The System

### Top-Level Files

| File | Size | Purpose |
|------|------|---------|
| `CLAUDE.md.template` | 3.1KB | Master config template. Variables: `{{PAI_VERSION}}`, `{{ALGO_PATH}}`. Defines 3 modes: NATIVE, ALGORITHM, MINIMAL. Context routing table at bottom. |
| `settings.json` | ~50KB | **Generated file** — compiled from config/*.jsonc by BuildSettings.ts. Contains env, hooks, permissions, identity, notifications, spinner verbs (430+), spinner tips (200+), counts, runtime state. |
| `manifest.json` | 204KB | SHA256 integrity manifest of all files. Generated by GenerateManifest.ts. |
| `install.sh` | 8.3KB | Bootstrap script: checks curl/git/bun, displays ASCII banner, launches PAI-Install/main.ts. |
| `statusline-command.sh` | ~2KB | Terminal status line display script. |

### Directory Map (11 directories, 1,156 total files)

```
.claude/
├── agents/          14 files,  140KB — Agent personality definitions
├── config/           8 files,   60KB — Source-of-truth config (JSONC)
├── hooks/           24 files,  432KB — Lifecycle event handlers
│   ├── handlers/     6 files          — Build-time handlers
│   └── lib/         17 files          — Shared hook utilities
├── lib/
│   └── migration/    5 files,   48KB — Version migration tools
├── MEMORY/           1 file,     4KB — Memory system (dirs created at runtime)
├── PAI/             23 files,  1.4MB — System docs, Algorithm, Tools, USER/
│   ├── Algorithm/    5 files          — v3.9.0 (current) + support docs (old versions removed)
│   ├── ACTIONS/     10 files          — Action system (runners, examples)
│   ├── FLOWS/        1 file           — Flow definitions
│   ├── PIPELINES/    2 files          — Pipeline definitions
│   ├── Tools/       40 files,  ~500KB — TypeScript utility tools
│   ├── USER/        12 dirs           — Personal data templates
│   └── dev/          5 files          — Architecture docs, audit, improvement tracking
├── PAI-Install/     25 files,  3.6MB — Installer (CLI + Web UI + Electron)
│   ├── cli/          3 files          — CLI installer
│   ├── engine/       8 files          — Install engine
│   ├── electron/     3 files          — Electron wrapper
│   ├── web/          2 files          — Web server/routes
│   └── public/      12 files          — Web UI (HTML/CSS/JS + fonts)
├── skills/         830 files,  ~16MB — 11 categories, 47 skills
│   ├── Utilities/  511 files,  3.4MB — Largest category
│   ├── Security/    90 files          — Recon, PromptInjection, WebAssessment, etc.
│   ├── Media/       73 files,  7.6MB — Art + Remotion (heaviest by size)
│   ├── Telos/       69 files          — Life OS / goal tracking
│   ├── Thinking/    53 files          — Council, RedTeam, FirstPrinciples, etc.
│   ├── Scraping/    32 files          — Apify + BrightData
│   ├── Agents/      26 files          — Agent composition system
│   ├── Investigation/ 22 files        — OSINT + PrivateInvestigator
│   ├── Research/    20 files          — Multi-source research
│   ├── USMetrics/    6 files          — US economic data
│   └── ContentAnalysis/ 3 files       — ExtractWisdom
└── tests/            7 files,   76KB — Test suite
```

---

## Core Systems

### 1. The Algorithm (PAI/Algorithm/)

The 7-phase execution engine: **Observe → Think → Plan → Build → Execute → Verify → Learn**

- **Current version:** v3.9.0 (pointed to by `LATEST` file)
- **Shipped versions:** v3.9.0 only (v3.5.0, v3.7.0, v3.8.0 removed in v4.4.1-dev cleanup)
- **Support docs:** CapabilitySelection.md, Examples.md, ISC-Methodology.md, MICRO.md
- **Concept:** Transition from CURRENT STATE to IDEAL STATE via verifiable criteria (ISC)
- **ISC = Ideal State Criteria = Verification Criteria** — binary, testable, 4-12 words each
- **Effort tiers:** Standard < Extended < Advanced < Deep < Comprehensive

### 2. Config System (config/)

Source-of-truth JSONC files compiled to settings.json by BuildSettings.ts:

| File | Contents |
|------|----------|
| `identity.jsonc` | daidentity (name, color, personality), principal (name, timezone) |
| `hooks.jsonc` | Hook registrations by lifecycle event + statusLine |
| `permissions.jsonc` | allow/deny/ask permission lists |
| `notifications.jsonc` | ntfy, Discord, Twilio routing |
| `preferences.jsonc` | env vars, memory retention, tech stack, MCP servers, dynamic context |
| `spinner-verbs.json` | 430+ custom loading verbs |
| `spinner-tips.json` | 200+ feature tips |
| `README.md` | Config system documentation |

**Build pipeline:** `config/*.jsonc` + `config/spinner-*.json` + `existing runtime state` → BuildSettings.ts → `settings.json`

### 3. Hook System (hooks/)

23 hook files across 8 lifecycle events:

**SessionStart:**
- `TerminalState.hook.ts` — Terminal state initialization
- `LoadContext.hook.ts` (18KB) — Dynamic context injection (relationship, learning, work)
- handlers/BuildCLAUDE.ts — Rebuild CLAUDE.md from template
- handlers/BuildSettings.ts (10KB) — Rebuild settings.json from config/

**UserPromptSubmit:**
- `ModeClassifier.hook.ts` — Classify effort tier
- `RatingCapture.hook.ts` (22KB) — Capture explicit/implicit ratings
- `TerminalState.hook.ts` — Track terminal state
- `UpdateTabTitle.hook.ts` — Set tab title
- `SessionAutoName.hook.ts` (22KB) — Auto-generate session names

**PreToolUse:**
- `SecurityValidator.hook.ts` (18KB) — Guard Bash/Edit/Write/Read
- `GitHubWriteGuard.hook.ts` — Guard GitHub write operations
- `TerminalState.hook.ts` — AskUserQuestion terminal state
- `AgentExecutionGuard.hook.ts` — Guard agent spawning (Task)
- `SkillGuard.hook.ts` — Guard skill invocations (Skill)

**PostToolUse:**
- `QuestionAnswered.hook.ts` — Reset tab after question answered
- `PRDSync.hook.ts` — Sync PRD frontmatter to work.json

**Stop:**
- `LastResponseCache.hook.ts` — Cache last response for ratings
- `TerminalState.hook.ts` — Reset terminal state
- `DocIntegrity.hook.ts` — Cross-reference validation
- `StopOrchestrator.hook.ts` — Orchestrate stop-phase hooks
- `AlgorithmTracker.hook.ts` — Track algorithm phase progress

**SessionEnd:**
- `WorkCompletionLearning.hook.ts` (11KB) — Extract learnings
- `SessionCleanup.hook.ts` (9KB) — Mark work complete, clear state
- `RelationshipMemory.hook.ts` (8KB) — Capture relationship notes
- `UpdateCounts.hook.ts` — Update system counts
- `IntegrityCheck.hook.ts` — PAI integrity validation

**Other events:**
- `ConfigChange.hook.ts` — React to config changes
- `PostCompactRecovery.hook.ts` — Recover context after compaction

**Shared libraries (hooks/lib/):** atomic.ts, change-detection.ts, classify.ts, hook-io.ts, identity.ts, learning-readback.ts, learning-utils.ts, notifications.ts, output-validators.ts, paths.ts, payload-schema.ts, prd-template.ts, prd-utils.ts, recovery-block.ts, tab-constants.ts, tab-setter.ts, time.ts

**Handlers (hooks/handlers/):** BuildCLAUDE.ts, BuildSettings.ts, DocCrossRefIntegrity.ts (33KB), SystemIntegrity.ts, TabState.ts, UpdateCounts.ts

### 4. Agent System (agents/)

14 specialized agents with consistent frontmatter format:

| Agent | Persona | Model | Key Role |
|-------|---------|-------|----------|
| Algorithm | Vera Sterling | opus | ISC management, Algorithm phases |
| Architect | — | — | System architecture |
| Artist | — | — | Visual content creation |
| BrowserAgent | — | — | Playwright browser automation |
| ClaudeResearcher | — | — | Claude-powered research |
| CodexResearcher | — | — | OpenAI Codex research |
| Designer | — | — | UX/UI design review |
| Engineer | Marcus Webb | opus | TDD, implementation, worktree isolation |
| GeminiResearcher | — | — | Gemini-powered research |
| GrokResearcher | — | — | Grok-powered research |
| Pentester | — | — | Security testing |
| PerplexityResearcher | — | — | Perplexity-powered research |
| QATester | — | — | Quality assurance, browser validation |
| UIReviewer | — | — | UI review |

**Common pattern:** All agents have frontmatter (name, description, model, permissions), mandatory startup sequence (load context files), identical output format (SUMMARY → STORY EXPLANATION → COMPLETED), and persona backstory.

### 5. Skill System (skills/)

11 categories, 47 SKILL.md files. Each skill has:
- `SKILL.md` with YAML frontmatter (name, description, use_when, workflows)
- `Workflows/` directory with one .md per workflow
- Optional `Tools/` directory with TypeScript utilities

**Categories by file count:**
1. Utilities (511 files, 3.4MB) — largest by far
2. Security (90 files) — Recon, PromptInjection, WebAssessment, SECUpdates, AnnualReports
3. Media (73 files, 7.6MB) — Art (image gen, diagrams), Remotion (video)
4. Telos (69 files) — Life OS, goals, projects
5. Thinking (53 files) — Council, RedTeam, FirstPrinciples, BeCreative, Science, IterativeDepth, WorldThreatModelHarness
6. Scraping (32 files) — Apify (social media), BrightData (web)
7. Agents (26 files) — Agent composition, traits, templates
8. Investigation (22 files) — OSINT, PrivateInvestigator
9. Research (20 files) — Multi-source research, Fabric patterns
10. USMetrics (6 files) — GDP, inflation, unemployment data
11. ContentAnalysis (3 files) — ExtractWisdom

### 6. Memory System (MEMORY/)

Runtime directories (created as needed):
- `LEARNING/` — Failure patterns, algorithm reflections, signals
- `RELATIONSHIP/` — Daily interaction patterns, preferences
- `STATE/` — Session names, algorithm state, caches
- `WORK/` — PRDs, transcripts, work context
- `WISDOM/` — Domain knowledge frames (dual-loop: OBSERVE reads, LEARN writes)

**PRD.md is the work system's single source of truth:**
```yaml
---
prd: true
id: PRD-20260207-auth
status: active
mode: ALGORITHM
effort_level: Extended
phase: Build
progress: 45
---
# [Task Title]
## Context
## Criteria
- [ ] ISC-1: Criterion description (8-12 words, binary, verifiable)
## Decisions
## Verification
```
PRDSync.hook.ts (PostToolUse) parses this frontmatter + criteria checkboxes on every Write/Edit and syncs to `MEMORY/WORK/{slug}/work.json`.

### 7. PAI Tools (PAI/Tools/)

~42 TypeScript tools organized by importance tier:

**Tier 1 — Core Runtime** (used every session or on demand):
- `algorithm.ts` (1,515 lines) — Algorithm CLI: loop, interactive, parallel modes, PRD management
- `pai.ts` (808 lines) — CLI entry point: launch, MCP management, version, profiles
- `upgrade.ts` (439 lines) — Install/upgrade/rollback engine
- `Inference.ts` — Anthropic API wrapper (haiku for speed, sonnet for quality)
- `BuildCLAUDE.ts` — Generate CLAUDE.md from template
- `GenerateManifest.ts` — SHA-256 integrity manifest builder
- `RebuildPAI.ts` — Regenerate compiled SKILL.md

**Tier 2 — Analysis & Memory** (session lifecycle, learning):
- `SessionHarvester.ts`, `SessionProgress.ts` — Session insight extraction
- `FailureCapture.ts` (554 lines) — Categorize and store failures
- `LearningPatternSynthesis.ts` — Synthesize learning patterns
- `RelationshipReflect.ts` (536 lines) — Relationship memory reflection
- `OpinionTracker.ts` — Track opinions/preferences
- `ActivityParser.ts` (688 lines), `TranscriptParser.ts` (418 lines) — Parse activity/transcripts

**Tier 3 — Wisdom System:**
- `WisdomCrossFrameSynthesizer.ts`, `WisdomDomainClassifier.ts`, `WisdomFrameUpdater.ts`

**Tier 4 — Display/UI** (banners, previews):
- `Banner.ts` (866 lines) — Primary banner (only one referenced by pai.ts)
- `BannerMatrix.ts` (693), `BannerNeofetch.ts` (598), `BannerRetro.ts` (728), `BannerTokyo.ts`, `BannerPrototypes.ts`, `NeofetchBanner.ts` (727) — Variant designs, not auto-loaded
- `PreviewMarkdown.ts`, `PAILogo.ts`

**Tier 5 — Utilities:**
- `IntegrityMaintenance.ts` (926 lines) — Scan + report + fix integrity issues
- `SecretScan.ts`, `GetCounts.ts`, `LoadSkillConfig.ts`, `FeatureRegistry.ts`
- `AlgorithmPhaseReport.ts`, `AddBg.ts`, `RemoveBg.ts`

**Tier 6 — Media/Transcription:**
- `SplitAndTranscribe.ts`, `ExtractTranscript.ts`, `GetTranscript.ts`, `YouTubeApi.ts`
- `extract-transcript.py` (Python — justified: faster-whisper CTranslate2 bindings)

**Tier 7 — Pipeline System:**
- `PipelineMonitor.ts` (602 lines), `PipelineOrchestrator.ts`
- `pipeline-monitor-ui/` — Full React+Vite monitoring app

**Tier 8 — Duplicate/Legacy:**
- `BuildCLAUDE.ts` — Also exists at `hooks/handlers/BuildCLAUDE.ts` (see IMPROVEMENT-INDEX §4.10)
- `PAI/ACTIONS/` — v1 runner (used by pai.ts) + v2 runner (used by actions/pipelines) both active

### 8. PAI-Install (PAI-Install/)

Complete installer system with 3 interfaces:
- **CLI** (`cli/`) — Terminal-based installer with prompts
- **Web UI** (`web/` + `public/`) — Browser-based installer with custom fonts
- **Electron** (`electron/`) — Desktop wrapper for web UI
- **Engine** (`engine/`) — Shared install logic (actions, detection, validation, steps, state)

### 9. Actions/Pipelines/Flows (PAI/ACTIONS/, PIPELINES/, FLOWS/)

**Actions:** Atomic units of work (LLM, shell, custom). Example actions provided.
- `lib/runner.ts` + `lib/runner.v2.ts` — Action execution engines
- `lib/types.ts` + `lib/types.v2.ts` — Type definitions
- `lib/pipeline-runner.ts` — Pipeline execution
- `pai.ts` — Arbol CLI

**Pipelines:** YAML-defined action chains with verification gates.
**Flows:** Scheduled source → pipeline → destination orchestration.

### 10. Tests (tests/)

7 test files covering:
- `AtomicWrite.test.ts` — Atomic file operations
- `BuildSettings.test.ts` — Settings compilation
- `Integration.test.ts` — Integration tests
- `ModeClassifier.test.ts` — Mode classification logic
- `PayloadSchema.test.ts` — Hook payload validation
- `PostCompactRecovery.test.ts` — Compaction recovery
- `Upgrade.test.ts` — Upgrade path testing

### 11. Migration Library (lib/migration/)

5 files for PAI version migration:
- `extractor.ts` (9.6KB) — Extract components from old versions
- `merger.ts` (9.5KB) — Merge old + new components
- `scanner.ts` (7.3KB) — Scan for migration targets
- `validator.ts` (11.7KB) — Validate migration results
- `index.ts` (269B) — Barrel export

---

## Data Flow

### Session Lifecycle

```
Session Start
  → TerminalState (init terminal)
  → LoadContext (inject relationship, learning, work context)
  → BuildCLAUDE (regenerate CLAUDE.md if needed)
  → BuildSettings (regenerate settings.json if config changed)

User Message
  → ModeClassifier (classify effort tier)
  → RatingCapture (capture ratings)
  → TerminalState (update)
  → UpdateTabTitle (set tab)
  → SessionAutoName (name session)

Tool Use
  → SecurityValidator (pre: guard Bash/Edit/Write/Read)
  → AgentExecutionGuard (pre: guard Task)
  → SkillGuard (pre: guard Skill)
  → PRDSync (post: sync PRD to work.json)

Response Complete
  → LastResponseCache (cache for ratings)
  → DocIntegrity (validate cross-refs)
  → StopOrchestrator (orchestrate stop hooks)
  → AlgorithmTracker (track phase progress)

Session End
  → WorkCompletionLearning (extract insights)
  → SessionCleanup (clear state)
  → RelationshipMemory (capture relationship notes)
  → UpdateCounts (update system counts)
  → IntegrityCheck (validate PAI integrity)
```

### Config Build Pipeline

```
config/identity.jsonc      ─┐
config/hooks.jsonc          │
config/permissions.jsonc    ├─→ BuildSettings.ts ─→ settings.json
config/notifications.jsonc  │
config/preferences.jsonc    │
config/spinner-verbs.json   │
config/spinner-tips.json   ─┘
```

### Context Loading

```
CLAUDE.md (loaded natively by Claude Code)
  ↓
loadAtStartup files (from settings.json):
  - PAI/AISTEERINGRULES.md
  - PAI/USER/AISTEERINGRULES.md
  - PAI/USER/PROJECTS/PROJECTS.md
  - PAI/USER/TELOS/DIGEST.md
  ↓
Dynamic context (LoadContext.hook.ts):
  - Relationship context (MEMORY/RELATIONSHIP/)
  - Learning readback (MEMORY/LEARNING/)
  - Active work summary (MEMORY/WORK/)
  ↓
On-demand context (CONTEXT_ROUTING.md):
  - System docs loaded as needed
  - USER/ context loaded as needed
```
