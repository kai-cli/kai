# v4.2.0 Development Checklist

Branch: `v4.2.0`
Milestone: https://github.com/kai-cli/Personal_AI_Infrastructure/milestone/1

---

## 1. Atomic writes for JSON state files ✅

**Already atomic (no work needed):**
- [x] `SessionAutoName` — `session-names.json` (pid-based tmp + renameSync)
- [x] `prd-utils.ts` — `work.json` (tmp + renameSync)

**Converted to atomic:**
- [x] Create `hooks/lib/atomic.ts` — `atomicWriteJSON(path, data)` + `atomicWriteText`
- [x] `SessionCleanup.hook.ts` — session-names.json + last-cleanup.json
- [x] `handlers/SystemIntegrity.ts` — integrity state JSON
- [x] `handlers/DocCrossRefIntegrity.ts` — review queue + drift state JSON
- [x] `handlers/UpdateCounts.ts` — usage cache + settings.json
- [x] `lib/tab-setter.ts` — tab state JSON files
- [x] Tests: `AtomicWrite.test.ts` (9 tests)

---

## 2. Algorithm micro-effort tier ✅

- [x] Add `Micro` tier to `PAI/Algorithm/v3.8.0.md` → bumped to `v3.9.0`
  - Budget: <30s | ISC: 1–4 | Min Capabilities: 0
  - Single bounded change, 1-3 files, no design decisions
  - Handled inline in CLAUDE.md — never loads Algorithm file
- [x] Update `CLAUDE.md` — effort tier pre-classification + inline Micro format
- [x] Lazy-load: Micro uses inline format; Standard+ loads `v3.9.0.md`
- [x] Updated `hooks/lib/recovery-block.ts` — bumped to v3.9.0
- [x] Updated `tests/PostCompactRecovery.test.ts` — v3.9.0 version assertion

---

## 3. Hook payload validation ✅

- [x] No external deps — hand-rolled structural validation (fast, zero overhead)
- [x] Create `hooks/lib/payload-schema.ts` — schemas for all 6 hook events:
  - `UserPromptSubmit`, `Stop`, `SessionStart`, `SessionEnd`, `PreToolUse`, `PostToolUse`
  - Fail-open: unknown events pass, missing optional fields warn, missing required fields → null
- [x] Update `hooks/lib/hook-io.ts` — validate after parse, log warnings/errors to stderr
- [x] Tests: `PayloadSchema.test.ts` (29 tests, 9 describe blocks)

---

## 4. Split monolithic settings.json ✅

- [x] Create `config/identity.jsonc`
- [x] Create `config/hooks.jsonc`
- [x] Create `config/permissions.jsonc`
- [x] Create `config/notifications.jsonc`
- [x] Create `config/preferences.jsonc`
- [x] Create `hooks/handlers/BuildSettings.ts` — merges config/ → settings.json with validation
- [x] Replace `_docs` fields with JSONC comments
- [x] Update `install.sh` to run BuildSettings on install
- [x] Write tests for BuildSettings merge + validation (26 tests)

---

## 5. Upgrade CLI (`pai upgrade`) ✅

- [x] Create `manifest.json` for v4.2.0 (file list + checksums — 1128 files)
- [x] Create `PAI/Tools/GenerateManifest.ts` — regenerates manifest.json for any release
- [x] Add `upgrade` command to existing `PAI/Tools/pai.ts` CLI entry point
- [x] Implement `upgrade` command via `PAI/Tools/upgrade.ts`:
  - [x] Auto-backup current `~/.claude` → `~/.claude-backup-{date}`
  - [x] Integrity validation against manifest.json (sha256 per file)
  - [x] Dependency check (Bun version, Claude Code presence)
  - [x] File installation with preserved paths (MEMORY, USER, identity.jsonc, MCPs)
  - [x] Post-upgrade migration hooks (BuildSettings.ts, BuildCLAUDE.ts)
  - [x] Auto-rollback on install failure
- [x] Write tests for backup, integrity check, rollback (31 tests)

---

## Release tasks

- [x] Create `Releases/v4.2.0/` snapshot from updated v4.1.0 files
- [x] Write `Releases/v4.2.0/CHANGELOG.md`
- [x] Update `Releases/v4.2.0/README.md`
- [x] Run full test suite — 143/143 pass
- [ ] Commit, push, PR → merge to fork main
