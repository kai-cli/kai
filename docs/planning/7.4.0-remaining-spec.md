# 7.4.0 — Remaining Work Spec

> **Status:** SPEC · created 2026-06-22 · the single "what's actually left in 7.4.0" map, built from
> **verified git state** (not roadmap checkboxes, which drifted during the multi-branch/Codex parallel work).
> Companion to `ROADMAP-7.x.md` 7.4.0 (the narrative) and `ada-build-spec.md` (ADA Group-A detail).

## Ground-truth correction (verified 2026-06-24 against origin/main)

This file began as a warning that roadmap checkboxes had drifted during the multi-branch/Codex parallel
work. That drift has since been resolved: the patch-now §5 work and the ready §0 native-event fixes are now
merged. Keep this page as the compact reconciliation map, not as an active claim that the old stranded
branches still need recovery.

| Item | Roadmap says | **Reality in main** | Where the code is |
|------|--------------|---------------------|-------------------|
| §0 payload-schema coverage + gate | ✅ | ✅ **in main** (PR #6) | merged |
| §0 AlgorithmTracker rewiring | ✅ | ✅ **in main** (PR #6) | merged |
| §5 verifier `--target` (SR-091) | `[ ]` | ✅ **in main** (PR #11) | recovered + merged |
| §5 literal scrubber (SR-093) | `[ ]` | ✅ **in main** (PR #11, perf batched in PR #24) | recovered + merged |
| §5 dependency-closure gate | ✅ | ✅ **in main** (PR #11, temp release gate in PR #23) | recovered + merged |
| §5c manifest regen | ✅ | ✅ **in main** | merged |
| §5 patch regression tests | ✅ | ✅ **in main** | merged |
| §0 Agent/native-field migration | `[ ]` | ✅ **in main** (PR #14) | merged |
| ADA T1/T2/T3 | partial | ✅ **in main** (PR #13/#15/#19) | merged |
| 7.4.2 CI/KAI gates | `[ ]` | ✅ **in main** (PR #19–#24) | merged |

**Action implied now:** do not chase the old stranded branches. Their intent is either shipped or tracked
as a later full-rebuild/design item below.

## The remaining work, by block

### Block §5 — KAI release hardening (CLOSEST to done)
**Patch-now items — DONE:** verifier `--target`, literal-safe scrubber, dependency-closure gate,
manifest/count regeneration, regression tests, temp KAI artifact gate, and scrub batching are all merged.
KAI public release is no longer blocked on the original patch-now gate.

**Full-rebuild items (deferred — `scoping`/`idea`, do after patch-now):**
- [ ] Sanitized staging artifact before live KAI mutation (SR-092)
- [ ] Single release/sync manifest (SR-087/090)
- [ ] Release/test gate wrapper — replace raw bun-output greps (SR-096/097)
- [ ] Executable public docs (SR-104)

### Block §0 — native-event migration (PARTIALLY done)
**Done in main:** payload-schema coverage + gate, AlgorithmTracker rewiring, AgentExecutionGuard /
AgentMemoryCapture `Task`→`Agent` migration, TaskCompleted / TeammateIdle / ConfigChange current-field
migration, and stderr-on-exit-2 behavior for blocking feedback.
- [x] **AgentExecutionGuard / AgentMemoryCapture: `Task` → `Agent`/subagent events** (SR-003) — shipped in PR #14.
- [x] **TaskCompleted / TeammateIdle / ConfigChange: read current fields** (SR-006/007) — shipped in PR #14.
- [x] **Skill telemetry via `UserPromptExpansion`** (SR-004) — direct `/slash` bypasses
      `PreToolUse:Skill`; shipped telemetry-only `UserPromptExpansion` wiring into `SkillTracker`.
- [ ] **PostToolBatch aggregation** (SR-099) — `idea`: replace race-prone per-tool aggregators.
- [ ] **InstructionsLoaded observability** (SR-038) — `idea`: replace prompt-time mtime polling.

### Block ADA — lead theme (DESIGN reconciled, Group A buildable)
Full detail: `ada-build-spec.md`. `ada-native-first.md` is retained as a historical design note:
its Claude-native import constraints still apply, but its hand-maintained-pack path is superseded
for Group A. The 3 gating decisions are now **answered** (fold into spec §7):
- Pack location: `~/.claude/ada/packs/` (+ `cache/` transient)
- First adopters: feed_bbf + feed_linksys (NOT PAI/KAI — recursion risk)
- Branch guards: hard-block `sysevent_integration`/`usp_ui`, warn-only `*`, +
  `ADA_BRANCH_GUARD_OVERRIDE=1` escape hatch that logs {repo, branch, expected, timestamp, action}.

- [x] **A1** — add REGISTRY rows (conventions/checkin_procedure/gotchas) for feed_bbf + feed_linksys
- [x] **A2** — `parseRegistry()` + unit tests
- [x] **A3** — `ada-generate.ts` (pack + gitignored `CLAUDE.local.md` pointer + procedure config + ≤120-line budget)
- [x] **A4** — drift gate `--check` + must-actually-fail test; wire into CI + weekly
- [x] **B1/B2 (T1/T2)** — pack content live + GitHubWriteGuard procedure-card/branch-guard extension shipped
      in PR #13/#15.
- [x] **B3 (T3 → 7.4.2)** — LocalContextFirst domain-match → inject retrieved content.
- [ ] **C** — human-confirmed `/ada-capture` (phase 1b); delegation injection for Explore/Plan + SDK/background
- [ ] **T4** — operational/env state — **phase 2, parked**

### Block — memory/observability spine (DECOUPLED)
Owned by `MEMORY-ARCHITECTURE-PLAN.md`; tracked here as pointers only:
- [ ] §1 Observability (telemetry, silent-degrade ×12, `/health`) — ADA's measurement substrate
- [ ] §2 cross-project deny-by-default scope model (immediate flag already shipped in 7.3.4) + agent harvest + MemCapture rule
- [ ] §3 SF-3 embeddings into MemoryRecall scorer
- [ ] §4 SF-35 sync-fidelity gate

## Recommended build order (dependency-aware)

1. **ADA private validation** — T1/T2/T3 are shipped; run real feed_bbf/feed_linksys flows and capture only
   evidence-backed follow-up fixes.
2. **Memory spine execution** — observability/scope/semantic recall work is owned by `MEMORY-SPINE-SPEC.md`.
3. **§5 full-rebuild items** — sanitized staging artifact, single release/sync manifest, release parser, executable docs.
4. **7.5.x subagent-context inheritance** — do not pull broad inheritance back into 7.4.x.

## What is NOT 7.4.0 (scope guard)
- T4 operational state → phase 2.
- §0 `PostToolBatch`/`InstructionsLoaded` → `idea`; keep as runtime-event observability unless a concrete
  memory/recall consumer is identified. `MEMORY-SPINE-SPEC.md` documents the boundary.
- Memory spine → its own plan/spec; 7.4.x only consumes its observability output for ADA measurement.
