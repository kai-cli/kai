# KAI v5.0.1 — Remaining Phase B-D Work

**Created:** 2026-04-28
**Context:** v5.0.0 shipped Phase A (fork + strip + history clean + CI + release infra). Phases B-D from the original PAI-5.0.0-PLAN.md have partial work done but aren't complete. This plan tracks what's left before moving to v5.1.0/v5.2.0.

---

## Status of Original Phases

### Phase A: Fork + Strip + History Clean — COMPLETE
Everything shipped. CI green (3 jobs). 367 tests. 15/15 verify-release checks. Symlink protections in place.

### Phase B: Config Layer + hooks/user/ — PARTIAL

**Done:**
- `config/domains.jsonc` exists with domain definitions
- 4 starter configs in `config/starters/` (fullstack, datascience, devops, generic)
- `hooks/lib/config-loader.ts` exists with tests (`ConfigLoader.test.ts`)
- `LocalContextFirst.hook.ts` refactored to read from `config/domains.jsonc`
- `hooks/lib/knowledge-readback.ts` partially wired (has comment referencing config-loader but still has hardcoded fallbacks)
- `config/user-hooks.jsonc.example` exists as documentation

**Remaining:**
- [ ] **B1.** `knowledge-readback.ts` — complete migration from hardcoded fallbacks to config-loader
- [ ] **B2.** `KnowledgeHarvester.ts` — externalize domain definitions to config-loader (R13 from original plan)
- [ ] **B3.** `hooks/user/` loading in BuildSettings.ts — implement actual merge of user hooks into settings.json (currently no code for this; only an example file exists)
- [ ] **B4.** Verify config-loader graceful degradation — if `domains.jsonc` is missing or empty, all hooks should no-op cleanly

### Phase C: Setup Wizard + Install — PARTIAL

**Done:**
- `PAI-Install/main.ts` — working 6-step wizard with identity config, Bedrock setup, USER scaffold, BuildSettings, BuildCLAUDE
- `import.meta.main` guard in place (critical safety fix from this session)
- `install.sh` — bootstrap script that installs Bun, launches main.ts
- `get-kai.sh` — curl-pipe downloader pointing to kai-cli/kai
- `config/identity.jsonc.template` exists
- Settings migration from existing `~/.claude` implemented

**Remaining:**
- [ ] **C1.** Archetype selection in wizard — plan calls for 4 archetypes (fullstack, datascience, devops, generic) that pre-populate `domains.jsonc` from starters. Current wizard doesn't offer this selection.
- [ ] **C2.** `CONTEXT_ROUTING.md` template generation — plan says generate from template; currently static
- [ ] **C3.** End-to-end test on clean machine — `get-kai.sh` on a machine with no `~/.claude` (the v5.0.0 plan's Phase C item 6)
- [ ] **C4.** Install URL finalization — PAI-5.0.0-PLAN.md prerequisite still unchecked: "Install URL: final repo URL locks the install.sh content and all docs"

### Phase D: Documentation — PARTIAL

**Done:**
- `CONTRIBUTING.md` exists in kai
- `CHANGELOG.md` exists with v5.0.0 entry
- `docs/CUSTOMIZATION.md` exists with config file table, domains guide, hooks/user/ instructions
- `docs/QUICKSTART.md` exists
- `docs/WHATS-DIFFERENT.md` exists

**Remaining:**
- [ ] **D1.** Review and polish `CUSTOMIZATION.md` — verify all instructions match actual implementation (especially hooks/user/ loading, which isn't implemented yet per B3)
- [ ] **D2.** Update `QUICKSTART.md` for archetype selection flow (once C1 is done)
- [ ] **D3.** Update `CHANGELOG.md` with v5.0.1 entry when this work ships

---

## v5.0.1 Punch List (ordered)

1. **B1** — knowledge-readback.ts config-loader migration
2. **B2** — KnowledgeHarvester.ts config-loader migration
3. **B3** — hooks/user/ loading mechanism in BuildSettings.ts
4. **B4** — config-loader graceful degradation verification
5. **C1** — archetype selection in installer wizard
6. **C2** — CONTEXT_ROUTING.md template generation
7. **C3** — end-to-end clean machine test
8. **C4** — install URL finalization
9. **D1** — CUSTOMIZATION.md accuracy review
10. **D2** — QUICKSTART.md update for archetype flow
11. **D3** — CHANGELOG.md v5.0.1 entry

**Estimated effort:** ~1 day focused work (B items are mechanical refactors, C1 is the biggest piece)

---

## Completion Gate

- [ ] All 11 items above done
- [ ] `bun test` passes (367+ tests)
- [ ] `bash scripts/verify-release.sh` passes in kai (15/15)
- [ ] CI green (all 3 jobs)
- [ ] Synced to kai via `sync-to-kai.sh --commit`
- [ ] PAI-5.0.0-PLAN.md remaining unchecked prerequisites resolved
