# Tasks

Use this to convert a plan into ordered tasks.

## Task Rules

- Each task must produce a concrete artifact: code, test, doc, config, or verification output.
- Order tasks by dependency, not by file type.
- Include a verification step for every risky behavior change.
- Keep follow-up ideas separate from required completion work.
