# PAI — Project Instructions

See `~/.claude/CLAUDE.md` for global PAI instructions. This project uses the global config.

Project-specific notes:
- Branch `v4.8.0-dev` is the active development branch
- v4.7.0 shipped: KnowledgeSync, Algorithm v3.11.0, context routing (PR #1 merged)
- v4.8.0 in progress: token optimization, staging, inference budget, pai curate, ReflectionHarvester
- Planning docs in `docs/planning/`
