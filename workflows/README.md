# PAI Workflow Templates

System-level workflow templates. Parameterized with `{{variable}}` interpolation.

See `scripts/workflow-run.ts` for the CLI runner and `PAI/SKILLSYSTEM.md` for the three-tier skill architecture.
